apiVersion: config.k8s.io/v1alpha1
kind: Krmfile
openAPI:
  definitions:
    io.k8s.cli.setters.registry-address:
      type: string
      description: Container registry address
      x-k8s-cli:
        setter:
          name: registry-address
          value: REGISTRY_ADDRESS
          required: true
    io.k8s.cli.substitutions.image:
      x-k8s-cli:
        substitution:
          name: image
          pattern: REGISTRY_ADDRESS/camel-kcp
          values:
          - marker: "REGISTRY_ADDRESS"
            ref: "#/definitions/io.k8s.cli.setters.registry-address"
